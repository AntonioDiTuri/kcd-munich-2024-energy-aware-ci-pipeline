name: GitHub Actions Demo
run-name: I am testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd demo/fibonacci && docker build . -t test
      - run: kind load docker-image test
      - run: cd demo/fibonacci && kubectl apply -f pod.yaml
      # - run: kubectl apply -f pod-ineff.yaml